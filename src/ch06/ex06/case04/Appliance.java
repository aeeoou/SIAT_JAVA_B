package ch06.ex06.case04;
// 인터페이스(interface) =
// implements
// 몽땅 추상메소드를 가지고 있으면 인터페이스다.
public interface Appliance {
	// 인터페이스 안에 있는 메서드는 접근제한자가 기본값으로 public 이다.
	void on();
	/*public abstract*/void off();
}
